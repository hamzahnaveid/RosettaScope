<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen"
    android:gravity="center"
    android:orientation="vertical"
    tools:context=".PainPointsActivity">

    <TextView
        android:id="@+id/textView_label_pp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="16dp"
        android:text="You will go through drills of Pain Points you have encountered so far in your language learning journey"
        android:textColor="@color/white"
        android:textSize="30sp" />

    <Button
        android:id="@+id/button_continue_pp"
        style="@android:style/Widget.Material.Button.Colored"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="8dp"
        android:background="@drawable/bg_text"
        android:onClick="toDrills"
        android:text="Continue"
        android:textAllCaps="false"
        android:textSize="20sp" />


</LinearLayout>